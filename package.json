{
  "name": "wannapachattong368",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "quasar dev",
    "build": "quasar build",
    "deploy": "npm run build && npx gh-pages -d dist/spa -b gh-pages",
    "test:playwright": "npx playwright test",
    "deploy-and-test": "npm run deploy && npm run test:playwright"
  },
  "dependencies": {
    "quasar": "^2.16.0",
    "vue": "^3.5.20",
    "vue-router": "^4.0.0",
    "axios": "^1.2.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "@quasar/app-vite": "^2.1.0",
    "gh-pages": "^6.3.0"
  }
}
